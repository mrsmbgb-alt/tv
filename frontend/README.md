# 📺 IPTV Streaming Frontend

Modern, responsive web interface for live TV streaming.

## 🚀 Quick Start

### 1. Configure API URL

Edit `js/api.js` and replace the backend URL:

```javascript
const API_CONFIG = {
    BASE_URL: 'https://your-backend.onrender.com', // YOUR BACKEND URL HERE
    // ...
};